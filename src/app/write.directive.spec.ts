import { WriteDirective } from './write.directive';

describe('WriteDirective', () => {
  it('should create an instance', () => {
    const directive = new WriteDirective();
    expect(directive).toBeTruthy();
  });
});
